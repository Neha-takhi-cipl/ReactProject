// import React from 'react';
// import { shallow } from 'enzyme';

// import PreferenceAdminBottom from '../index';

describe('<PreferenceAdminBottom />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
